// import { ScrollListener } from "../utils/ScrollListener";
// import { fireEvent } from '@testing-library/react';


// const mockSetStateTest = jest.fn()

// jest.mock('react', () => ({
//     useState: setStateTest => [setStateTest, mockSetStateTest]
// }));

test('scroll pass above 70, state change to statechange', async function () {

    // const test = ScrollListener(mockSetStateTest, 'test-origine', 'test-change')

    // await fireEvent.scroll(window, { target: { scrollY: 71 } })

    // expect(mockSetStateTest).toHaveBeenCalledWith('test-change')

    // await fireEvent.scroll(window, { target: { scrollY: 39 } })

    // expect(mockSetStateTest).toHaveBeenCalledWith('test-origine')
})
